name: Update progress tracker

on:
  push:
    branches: [main]
    paths:
      - "Data Structures & Algorithms/**"
      - "scripts/neetcode250.json"
      - "scripts/generate_progress.py"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generate progress section
        run: python3 scripts/generate_progress.py

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if ! git diff --quiet -- README.md; then
            git add README.md
            git commit -m "chore: update progress tracker [skip ci]"
            git push
          else
            echo "No changes to commit."
          fi
